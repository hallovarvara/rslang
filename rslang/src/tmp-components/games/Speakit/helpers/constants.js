const amountOfWordsOnOnePage = 10;
const pagesCount = 30;
const levelsCount = 6;
const apiLinks = {
  file: 'https://raw.githubusercontent.com/hallovarvara/rslang-data/master/',
};
const localStorageItems = {
  latestResults: 'latestResults',
};
const maxLatestResultsCount = 10;

export {
  amountOfWordsOnOnePage,
  pagesCount,
  levelsCount,
  apiLinks,
  localStorageItems,
  maxLatestResultsCount,
};
