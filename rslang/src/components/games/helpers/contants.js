const pagesCount = 30;
const levelsCount = 6;
const wordsPerPage = 10;

const localStorageItems = {
  latestResults: 'unmess_latest-results',
};

export {
  pagesCount,
  levelsCount,
  wordsPerPage,
  localStorageItems,
};
